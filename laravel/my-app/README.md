This Laravel app is simple CRUD for Member table.
We use different factory methods to generate different type of users from factory class.
We use custom validations here.
We use Route Model binding here.
We use Request class for form validations.

## What is not included:

Middleware is missing
Table Relationship is missing
Soft Delete is missing
